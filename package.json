{
  "name": "viral-farm-prototype",
  "version": "0.4.1",
  "private": true,
  "description": "SNS 바이럴 포지션 게임의 모바일 웹 프로토타입",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js && node tests/test-logic.cjs"
  }
}
